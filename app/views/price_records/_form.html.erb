<%= form_for(@price_record) do |f| %>
  <% if @price_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price_record.errors.count, "error") %> prohibited this price_record from being saved:</h2>

      <ul>
      <% @price_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :psy_price %><br>
    <%= f.number_field :psy_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
